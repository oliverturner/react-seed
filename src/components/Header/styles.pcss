.main {
  display:        flex;
  padding-bottom: 1em;
  border-bottom:  1px solid #eee;
  margin-bottom:  1em;
}

.logo {
  margin-right: 20px
}

.wrap {
  display:         flex;
  flex-flow:       column;
  justify-content: center;
}

.title {
  margin-bottom: .5em;
  font-weight:   bold;
  font-size:     2em;
}

.tagline {
  font-weight: normal;

  strong {
    font-weight: bold
  }
}
